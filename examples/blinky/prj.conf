# Blinky Project Configuration
# For nRF9151 Feather / Myriota HyperPulse Dev Kit

# Enable GPIO
CONFIG_GPIO=y

# Enable logging (optional, useful for debugging)
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3

# Console output via UART (optional)
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
